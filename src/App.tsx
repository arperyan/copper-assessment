import styles from "./App.module.css";
import Layout from "./components/Layout";

const App = () => {
    return (
        <div className={styles.wrapper}>
            <Layout />
        </div>
    );
};

export default App;
